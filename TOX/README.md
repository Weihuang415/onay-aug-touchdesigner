# TOX components

Most `.tox` files are tracked in Git.

**`MediaPipe.tox`** (~172 MB) is **not** in the repository (GitHub file size limit).

Copy it manually from another machine or your backup when setting up a new PC.
